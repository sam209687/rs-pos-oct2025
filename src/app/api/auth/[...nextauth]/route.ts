// src/app/api/auth/[...nextauth]/route.ts

// Change this line:
import { GET, POST } from "@/lib/auth"; // <--- Import GET and POST directly

// The rest of the file is correct after this change:
export { GET, POST };