import { TableSkeleton } from "@/components/skeletons/TableSkeleton";

export default function Loading() {
  // This UI will be shown automatically while the brand table page loads.
  return <TableSkeleton />;
}