import { FormSkeleton } from "@/components/skeletons/FormSkeleton";

export default function Loading() {
  // This UI will be shown automatically while the "Add Brand" form page loads.
  return <FormSkeleton />;
}